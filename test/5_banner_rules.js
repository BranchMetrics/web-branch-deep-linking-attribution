'use strict';

goog.require('banner_rules');

describe('Banner Rules', function() {
	var assert = testUtils.unplanned();

	beforeEach(function() {
	});

	afterEach(function() {
	});

	it('TODO', function() {
		assert.ok(banner_rules, 'TODO');
	});
});

